<view class="title">
  <view class="{{item.isActive ? 'active' : ''}}" wx:for="{{Tabs}}" wx:key="id" bindtap="tabsTap" data-id="{{item.id}}">
    {{item.name}}
  </view>
</view>
<view class="content">
  <slot></slot>
</view>